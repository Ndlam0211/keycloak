server:
  port: 8080
  servlet:
    context-path: /profile
spring:
  application:
    name: profile-service

  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: http://localhost:8180/realms/lamnd

  data:
    mongodb:
      uri: mongodb://root:root@localhost:27017/profile-service?authSource=admin

idp:
  url: http://localhost:8180
  client-id: lamnd_app
  client-secret: JVQqedIUbd1hfeexiA7t3wocvM46ByNQ
